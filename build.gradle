allprojects {
    apply plugin: 'idea'
}

subprojects {
    repositories {
        mavenCentral()
    }
    apply plugin: 'java'
    sourceCompatibility  = 1.8
}

task wrapper(type: Wrapper) {
    gradleVersion = '3.0'
}
