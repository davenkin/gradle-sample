allprojects {
    apply plugin: 'idea'
    apply plugin: 'eclipse'
}

subprojects {
    repositories {
        mavenCentral()
    }
    apply plugin: 'java'
    sourceCompatibility  = 1.8
}

task wrapper(type: Wrapper) {
    gradleVersion = '3.0'
}
